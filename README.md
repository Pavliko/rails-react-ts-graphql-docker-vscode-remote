# Rails + React + TS + GraphQL + Docker + VSCode Remote Example

Setup:
```
docker-compose build

make runner

$ /app# bundle exec rails db:create
```

Run in dev:
```
docker-compose up --build rails webpacker
open http://localhost:3003
```
